enum VerificationReason {
  firstTimeLogin,           // First login attempt by user
  deviceChanged,            // Detected change in device
  loginConfirmedOnNewDevice    // User confirmed login on new device
}
